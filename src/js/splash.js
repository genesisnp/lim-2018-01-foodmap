// setTimeout(() => {

//   window.location.assign("/src/html/principal.html");
// }, 2000)

window.onload=timeout;
function timeout(){
window.setTimeout("redirect()",2000)}

function redirect(){
window.location="/src/html/principal.html"
return}